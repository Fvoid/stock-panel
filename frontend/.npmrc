# 改用官方源实测：单次小请求能连通，但真正 install 时对 registry.npmjs.org
# 的连接会建立后卡住不传数据(国内出口对该域名批量下载不稳定属已知现象)。
# 换回国内镜像,并加大超时/重试,容忍镜像偶发的短暂卡顿。
registry=https://registry.npmmirror.com/
network-timeout=300000
fetch-retries=5
fetch-retry-mintimeout=5000
fetch-retry-maxtimeout=60000
